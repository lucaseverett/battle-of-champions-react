import { css } from "@emotion/css";

const styles = css`
  position: relative;

  input {
    text-indent: 30px;
  }

  .material-icons {
    position: absolute;
    top: 8px;
    left: 10px;
    color: var(--input-search-icon-text-color);
  }
`;

export default styles;
